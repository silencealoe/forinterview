/**
 * 当一个字符串 s 包含的每一种字母的大写和小写形式
 *  同时 出现在 s 中，就称这个字符串 s 是 美好 字符串。
 * 比方说，"abABB" 是美好字符串，因为 'A' 和 'a' 同时出现了，且 'B' 和 'b' 也同时出现了。
 * 然而，"abA" 不是美好字符串因为 'b' 出现了，而 'B' 没有出现。
*/

var longestNiceSubstring = function(s) {
    let resStr = '';
    for(let i = 0; i<s.length;i++){
        // if(s[i+1])
    }
};